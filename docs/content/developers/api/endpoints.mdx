import { Callout } from 'nextra/components'

# API Endpoints

<Callout type="info">
This overview has been superseded by the **[full API Reference](/reference)** — a complete,
always-up-to-date reference generated from the live OpenAPI specification, with request and
response schemas, cURL/JavaScript/Python examples and an in-browser playground.
</Callout>

All endpoints are prefixed with `/api/v1/`. The [API Reference](/reference) documents every
public endpoint group:

- **[Authentication](/reference/auth)** — login, refresh, logout
- **[API Tokens](/reference/api-tokens)** — create and manage `lh_` tokens
- **[Organizations](/reference/orgs)**, **[Users](/reference/users)**, **[User Groups](/reference/usergroups)**
- **[Courses](/reference/courses)**, **[Chapters](/reference/chapters)**, **[Activities](/reference/activities)**, **[Blocks](/reference/blocks)**
- **[Assignments](/reference/assignments)** — including [headless grading flows](/developers/api/headless-assignments)
- **[Folders](/reference/folders)**, **[Media](/reference/media)**, **[Certifications](/reference/certifications)**
- **[Payments](/reference/payments)**, **[Search](/reference/search)**, **[Analytics](/reference/analytics)**
- **[Webhooks](/reference/webhooks)** and **[Headless](/reference/headless)** server-to-server endpoints

## Common Patterns

- All endpoints are prefixed with `/api/v1/`.
- Resources follow standard REST conventions: `GET` to list or retrieve, `POST` to create, `PUT` to update, `DELETE` to remove.
- Create/update endpoints commonly accept **multipart form data** rather than JSON, since many also accept file uploads (e.g. thumbnails, avatars).
- Authenticated endpoints require either the `Authorization: Bearer <token>` header or the `access_token_cookie` cookie.
- Programmatic access uses API tokens with the `lh_` prefix (Pro plan).
- Responses are returned as JSON.

For a local development instance, the interactive Swagger UI also remains available at
[http://localhost:1338/docs](http://localhost:1338/docs).
