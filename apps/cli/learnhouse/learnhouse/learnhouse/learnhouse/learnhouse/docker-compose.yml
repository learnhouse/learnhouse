version: "3.9"

name: learnhouse-147377dc

services:
  learnhouse-app:
    image: ghcr.io/learnhouse/app:latest
    container_name: learnhouse-app-147377dc
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # HOSTNAME needs to be set explicitly for the container
      - HOSTNAME=0.0.0.0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - learnhouse-network-147377dc
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  learnhouse-collab:
    image: ghcr.io/learnhouse/collab:latest
    container_name: learnhouse-collab-147377dc
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - COLLAB_PORT=4000
      - LEARNHOUSE_API_URL=http://learnhouse-app-147377dc:9000
    depends_on:
      learnhouse-app:
        condition: service_healthy
    networks:
      - learnhouse-network-147377dc

  nginx:
    image: nginx:alpine
    container_name: learnhouse-nginx-147377dc
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-80}:80"
    volumes:
      - ./extra/nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      learnhouse-app:
        condition: service_healthy
      learnhouse-collab:
        condition: service_started
    networks:
      - learnhouse-network-147377dc
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  db:
    image: postgres:16-alpine
    container_name: learnhouse-db-147377dc
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-learnhouse}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-learnhouse}
      - POSTGRES_DB=${POSTGRES_DB:-learnhouse}
    volumes:
      - learnhouse_db_data_147377dc:/var/lib/postgresql/data
    networks:
      - learnhouse-network-147377dc
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-learnhouse}"]
      interval: 5s
      timeout: 4s
      retries: 5

  redis:
    image: redis:8.6.1-alpine
    container_name: learnhouse-redis-147377dc
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - learnhouse_redis_data_147377dc:/data
    networks:
      - learnhouse-network-147377dc
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 4s
      retries: 5

networks:
  learnhouse-network-147377dc:
    driver: bridge

volumes:
  learnhouse_db_data_147377dc:
  learnhouse_redis_data_147377dc:
